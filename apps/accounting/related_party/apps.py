from django.apps import AppConfig

class RelatedPartyConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.accounting.related_party'
    label = 'accounting_related_party'
